#pragma once
#include <vector>
#include "Grid.h"
#include "Cell.h"

/*class Game {
	int turn_ = 0;

	vector<vector<Cell>> init_;
	vector<vector<Cell>> past_;
	vector<vector<Cell>> current_;
	vector<vector<Cell>> next_;

public:


	void game(vector<vector<Cell>> init);
	void setInit(vector<vector<Cell>> init);
	void setPast();
	void setCurrent();
	void setNext();

	void rules(int i, int j);
};*/

